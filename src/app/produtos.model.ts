export class Produtos {
    id: string;
    name: string;
    description: string;
    basePrice: number; // number em TS suporta decimal
    stock: number;
    // discount: number;
    // imageUrl: string;
    createdAt: string;
    updatedAt: string;
}
